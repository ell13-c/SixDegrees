---
phase: 22-ego-map-api-and-compatibility-serving
plan: 03
type: execute
wave: 2
depends_on:
  - 22-01
  - 22-02
files_modified:
  - backend/routes/map.py
  - backend/tests/conftest.py
  - backend/tests/test_contracts.py
autonomous: true
requirements:
  - EGO-05
  - COMP-01
  - COMP-03
must_haves:
  truths:
    - "Authenticated users cannot read another user's ego map payload."
    - "Map API response remains backward-compatible while adding non-breaking suggestion metadata."
    - "End-to-end route output uses ego-projected nodes derived from global coordinates."
  artifacts:
    - path: "backend/routes/map.py"
      provides: "Self-only ego map serving route"
      exports: ["get_map", "trigger_map"]
    - path: "backend/tests/conftest.py"
      provides: "Deterministic map/profile RPC fixtures for ego behavior tests"
      contains: "get_global_map_coordinates"
    - path: "backend/tests/test_contracts.py"
      provides: "Map API contract regression tests for auth + compatibility"
      contains: "test_get_map"
  key_links:
    - from: "backend/routes/map.py"
      to: "backend/services/map_pipeline/ego_map.py"
      via: "build_ego_map invocation"
      pattern: "build_ego_map"
    - from: "backend/routes/map.py"
      to: "backend/sql/v2_phase22_ego_profile_projection.sql"
      via: "get_ego_map_profiles RPC"
      pattern: "get_ego_map_profiles"
    - from: "backend/tests/test_contracts.py"
      to: "backend/routes/map.py"
      via: "FastAPI endpoint contract assertions"
      pattern: "/map/"
---

<objective>
Wire ego-map serving into the map route while preserving compatibility and enforcing access controls.

Purpose: Deliver user-visible phase behavior (safe requester-centered map payloads) without frontend changes.
Output: Route integration, fixture updates, and contract tests proving auth and compatibility guarantees.
</objective>

<execution_context>
@/Users/BAEK/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/BAEK/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-ego-map-api-and-compatibility-serving/22-RESEARCH.md
@backend/routes/map.py
@backend/tests/conftest.py
@backend/tests/test_contracts.py

<interfaces>
From backend/routes/map.py:
```python
@router.get("/{user_id}")
async def get_map(user_id: str, acting_user_id: str = Depends(get_current_user)):
    ...
```

From backend/tests/test_contracts.py:
```python
def test_get_map_response_shape(client):
    ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate ego projection and self-only access in map route</name>
  <files>backend/routes/map.py</files>
  <action>Update `GET /map/{user_id}` to enforce self-only reads (`acting_user_id` must equal `user_id`, otherwise 403). Replace raw global coordinate response assembly with `build_ego_map` integration using `get_global_map_coordinates` rows plus `get_ego_map_profiles` projection rows. Preserve top-level response keys (`user_id`, `version_date`, `computed_at`, `coordinates`) and existing coordinate entry keys while adding non-breaking `is_suggestion` metadata.</action>
  <verify>
    <automated>cd backend && ./venv/bin/python -m pytest -q tests/test_contracts.py::test_get_map_no_jwt_returns_401 tests/test_contracts.py::test_get_map_other_user_returns_403</automated>
  </verify>
  <done>Map route serves requester-centered ego payloads, denies cross-user reads, and maintains compatibility contract shape.</done>
</task>

<task type="auto">
  <name>Task 2: Update map fixtures and contract regressions for compatibility guarantees</name>
  <files>backend/tests/conftest.py, backend/tests/test_contracts.py</files>
  <action>Extend mock Supabase RPC routing to provide deterministic multi-user global coordinate rows and ego profile projection rows (with reciprocal and non-reciprocal friend cases). Update/add map contract tests to assert requester at `(0,0)`, mutual-only primary nodes, bounded suggestion fallback with `is_suggestion=true`, and backward-compatible field presence expected by frontend consumers. Keep tests deterministic and offline.</action>
  <verify>
    <automated>cd backend && ./venv/bin/python -m pytest -q tests/test_contracts.py -k map</automated>
  </verify>
  <done>Map contract tests prove ego behavior, compatibility, and security constraints without requiring frontend changes.</done>
</task>

</tasks>

<verification>
Run map contract subset and confirm route behavior satisfies ego centering, mutual filtering, suggestion fallback, and auth constraints.
</verification>

<success_criteria>
- Cross-user map reads are denied with explicit 403.
- Successful map responses remain frontend-compatible and include requester-centered ego coordinates.
- Contract tests pass for origin, mutuality, suggestion metadata, and auth behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/22-ego-map-api-and-compatibility-serving/22-03-SUMMARY.md`
</output>
