---
phase: 22-ego-map-api-and-compatibility-serving
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/sql/v2_phase22_ego_profile_projection.sql
  - backend/tests/test_phase22_ego_profile_projection_sql.py
autonomous: true
requirements:
  - EGO-02
  - EGO-04
  - COMP-02
must_haves:
  truths:
    - "Map-serving code can derive reciprocal friendship relationships from a dedicated profile projection contract."
    - "Suggestion candidates can be sourced without exposing sensitive profile fields in serving payloads."
    - "Profile projection behavior is regression-guarded by automated tests."
  artifacts:
    - path: "backend/sql/v2_phase22_ego_profile_projection.sql"
      provides: "RPC projection contract for ego-map profile reads"
      contains: "get_ego_map_profiles"
    - path: "backend/tests/test_phase22_ego_profile_projection_sql.py"
      provides: "SQL contract checks for allowed projection fields"
      contains: "test_ego_profile_projection"
  key_links:
    - from: "backend/routes/map.py"
      to: "backend/sql/v2_phase22_ego_profile_projection.sql"
      via: "RPC call for profile projection"
      pattern: "get_ego_map_profiles"
    - from: "backend/tests/test_phase22_ego_profile_projection_sql.py"
      to: "backend/sql/v2_phase22_ego_profile_projection.sql"
      via: "text contract assertions"
      pattern: "RETURNS TABLE"
---

<objective>
Define a safe profile projection contract for ego-map serving.

Purpose: Enable mutual-friend and suggestion logic to use explicit allow-listed profile fields instead of broad profile payloads.
Output: SQL RPC contract and deterministic SQL contract tests for ego profile projection.
</objective>

<execution_context>
@/Users/BAEK/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/BAEK/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-ego-map-api-and-compatibility-serving/22-RESEARCH.md
@backend/sql/v2_phase20_map_coordinates_global_contract.sql

<interfaces>
From backend/sql/v2_phase20_map_coordinates_global_contract.sql:
```sql
CREATE OR REPLACE FUNCTION public.get_global_map_coordinates(
    p_user_ids UUID[] DEFAULT NULL,
    p_version_date DATE DEFAULT NULL
)
RETURNS TABLE (
    user_id UUID,
    x DOUBLE PRECISION,
    y DOUBLE PRECISION,
    prev_x DOUBLE PRECISION,
    prev_y DOUBLE PRECISION,
    computed_at TIMESTAMPTZ,
    version_date DATE
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ego-map profile projection RPC contract</name>
  <files>backend/sql/v2_phase22_ego_profile_projection.sql</files>
  <action>Create an idempotent SQL migration that defines `get_ego_map_profiles` as a SECURITY DEFINER read RPC returning only allow-listed fields required by ego serving (at minimum: `id`, `nickname`, `friends`). Do not include sensitive fields (`age`, `education`, `languages`, `occupation`, or raw interaction counters) in the RPC return table. Keep grants aligned with existing backend authenticated/service-role patterns.</action>
  <verify>
    <automated>cd backend && ./venv/bin/python -m pytest -q tests/test_phase22_ego_profile_projection_sql.py::test_ego_profile_projection_rpc_exists</automated>
  </verify>
  <done>The SQL migration contains a callable `get_ego_map_profiles` RPC with an explicit allow-listed RETURNS TABLE signature suitable for ego map serving.</done>
</task>

<task type="auto">
  <name>Task 2: Add SQL contract tests for projection safety and shape</name>
  <files>backend/tests/test_phase22_ego_profile_projection_sql.py</files>
  <action>Add deterministic SQL text contract tests that assert: (1) migration defines `get_ego_map_profiles`, (2) return columns include required serving fields for mutuality, and (3) prohibited sensitive columns are absent from the RETURNS TABLE declaration. Keep tests offline and file-based like existing migration contract tests.</action>
  <verify>
    <automated>cd backend && ./venv/bin/python -m pytest -q tests/test_phase22_ego_profile_projection_sql.py</automated>
  </verify>
  <done>Contract test file fails if projection schema drifts or sensitive fields are added accidentally.</done>
</task>

</tasks>

<verification>
Run the new SQL contract test file and confirm projection RPC is constrained to serving-safe columns.
</verification>

<success_criteria>
- Ego map serving has a dedicated profile projection RPC with explicit allow-listed fields.
- SQL contract tests guard against sensitive-field leakage and signature drift.
- Phase 22 consumers can read friendship projection data without broad profile reads.
</success_criteria>

<output>
After completion, create `.planning/phases/22-ego-map-api-and-compatibility-serving/22-01-SUMMARY.md`
</output>
