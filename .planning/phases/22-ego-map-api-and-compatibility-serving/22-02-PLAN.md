---
phase: 22-ego-map-api-and-compatibility-serving
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/services/map_pipeline/contracts.py
  - backend/services/map_pipeline/ego_map.py
  - backend/tests/map_pipeline/test_ego_map.py
autonomous: true
requirements:
  - EGO-01
  - EGO-02
  - EGO-03
  - EGO-04
must_haves:
  truths:
    - "Requester is placed at exact (0,0) in transformed ego coordinates."
    - "Primary returned nodes are restricted to reciprocal friends."
    - "Friend and suggestion nodes use requester-relative translated coordinates."
    - "Sparse/empty mutual graphs receive bounded fallback suggestions marked `is_suggestion=true`."
  artifacts:
    - path: "backend/services/map_pipeline/contracts.py"
      provides: "Typed ego-map serving contracts"
      contains: "EgoMapNode"
    - path: "backend/services/map_pipeline/ego_map.py"
      provides: "Pure ego-map projection builder"
      contains: "build_ego_map"
    - path: "backend/tests/map_pipeline/test_ego_map.py"
      provides: "Deterministic unit tests for ego-map transformation"
      contains: "test_build_ego_map"
  key_links:
    - from: "backend/services/map_pipeline/ego_map.py"
      to: "backend/services/map_pipeline/contracts.py"
      via: "typed request/response objects"
      pattern: "EgoMap"
    - from: "backend/tests/map_pipeline/test_ego_map.py"
      to: "backend/services/map_pipeline/ego_map.py"
      via: "pure function assertions"
      pattern: "build_ego_map"
---

<objective>
Implement deterministic ego-map projection logic in an isolated service module.

Purpose: Separate mutuality/translation/suggestion business logic from route plumbing so correctness is testable before API wiring.
Output: Typed contracts, pure builder implementation, and deterministic unit tests.
</objective>

<execution_context>
@/Users/BAEK/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/BAEK/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-ego-map-api-and-compatibility-serving/22-RESEARCH.md
@backend/services/map_pipeline/contracts.py
@backend/routes/map.py

<interfaces>
From backend/routes/map.py:
```python
def _fetch_map_response(user_id: str) -> dict:
    ...
```

From backend/services/map_pipeline/contracts.py:
```python
RawInteractionCounts = dict[tuple[str, str], dict[str, int]]
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define ego-map interfaces and RED tests</name>
  <files>backend/services/map_pipeline/contracts.py, backend/tests/map_pipeline/test_ego_map.py</files>
  <action>Add explicit ego-map contract types (input coordinate row, profile projection row, output node shape with `is_suggestion`) in `contracts.py`, then create failing unit tests in `test_ego_map.py` that encode required behavior for requester origin, reciprocal-friend filtering, coordinate translation, bounded suggestion fallback, and deterministic suggestion ordering.</action>
  <verify>
    <automated>cd backend && ./venv/bin/python -m pytest -q tests/map_pipeline/test_ego_map.py::test_requester_is_forced_to_origin</automated>
  </verify>
  <done>Contracts are exported and at least one ego-map behavior test fails against missing implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Implement GREEN ego projection builder</name>
  <files>backend/services/map_pipeline/ego_map.py</files>
  <action>Implement `build_ego_map` as a pure function that: locates requester global coordinate, translates candidate nodes relative to requester, includes requester at exact `(0,0)`, keeps only reciprocal friends as primary nodes, and adds bounded fallback suggestions when mutual count is sparse/empty. Mark fallback rows with `is_suggestion=true`; mutuals/requester must be `false` or omitted by contract default. Keep output deterministic and side-effect free.</action>
  <verify>
    <automated>cd backend && ./venv/bin/python -m pytest -q tests/map_pipeline/test_ego_map.py::test_filters_to_mutual_friends_only tests/map_pipeline/test_ego_map.py::test_adds_bounded_suggestions_when_mutuals_sparse</automated>
  </verify>
  <done>All ego-map unit tests pass and output shape is ready for direct route consumption.</done>
</task>

<task type="auto">
  <name>Task 3: REFACTOR for edge cases and full deterministic suite</name>
  <files>backend/services/map_pipeline/ego_map.py, backend/tests/map_pipeline/test_ego_map.py</files>
  <action>Refactor implementation to handle missing requester row, absent friend lists, and empty coordinate/profile inputs with explicit exceptions or empty-safe behavior required by tests. Keep function signatures stable for route integration and avoid introducing Supabase calls inside this module.</action>
  <verify>
    <automated>cd backend && ./venv/bin/python -m pytest -q tests/map_pipeline/test_ego_map.py</automated>
  </verify>
  <done>Ego-map module is production-ready, deterministic, and fully unit-tested without external I/O.</done>
</task>

</tasks>

<verification>
Run full ego-map unit suite and confirm all requirements encoded by tests pass in the backend virtualenv.
</verification>

<success_criteria>
- Pure ego-map module reliably enforces origin, mutual filtering, translation, and suggestion fallback behavior.
- All behavior is covered by deterministic unit tests and can be consumed by API route wiring directly.
- No Supabase/network dependency exists in ego-map business logic.
</success_criteria>

<output>
After completion, create `.planning/phases/22-ego-map-api-and-compatibility-serving/22-02-SUMMARY.md`
</output>
